services:
  app:
    image: ardfordev/maven-app:${IMAGE_TAG}
    container_name: maven-app
    restart: unless-stopped
    ports:
      - "80:80"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge